# ep-{YYYYMMDD}-{slug}: {태스크명}

**Status:** [Draft]

> 이 파일을 복사해 `docs/plans/active/ep-{YYYYMMDD}-{slug}.md`로 저장하세요.
> 당신은 무맥락(stateless) 에이전트일 수 있습니다 — 과거 대화에 의존하지 말고
> 이 파일과 코드베이스만으로 작업을 완수할 수 있게 작성하세요.
> `**Status:**` 줄과 `## Affected Files`, `## Approval`의 형식은 pre-commit
> 훅이 파싱하므로 구조를 바꾸지 마세요. 규칙: AGENTS.md 계약 구역.

## Purpose

<!-- 왜 이 변경이 필요한가. 완료 후 무엇을 보면 동작을 확인할 수 있는가. 1-3줄. -->

## Affected Files

<!-- 변경할 소스 파일의 저장소 루트 기준 상대 경로. 한 줄에 하나.
     디렉터리 전체는 끝에 / (예: src/auth/).
     여기 없는 소스 파일은 수정 불가 — 필요해지면 추가 후 재승인. -->
- src/example.ts

## Acceptance Criteria

<!-- 인간이 검증 가능한 구체적 조건. 체크박스로. -->
- [ ] {테스트 가능한 기준 1}

## Approval

<!-- 승인 시: Approver input: "인간 입력 원문" (따옴표 필수 — 훅이 파싱) · Time: {ISO8601} -->
- Approver input: (대기)
- Time: (대기)

## Change Log

<!-- 소스 변경마다 즉시 1줄: {ISO8601} {CREATE|MODIFY|DELETE} {경로} — {Why 한 줄} -->

## Run History

<!-- 실행 종료마다(abort 포함) 1줄: {ISO8601} {절차명} {DONE|ABORTED|FAILED} — {한 줄} -->
