# HARNESS.md

Frameness는 AI 에이전트가 코드를 수정하기 전에 **사람이 무엇을 왜 바꾸는지 승인**하게 하고 모든 계획, 승인, 검증, 의사결정 과정을 계획 파일에 기록해 세션이 끊겨도 **재현 가능하고 추적 가능한 개발 프로세스**를 만드는 것을 목표로 합니다.

## 무슨 문제를 어떻게 해결했는가

### 1. 기존 하네스 문제

내 프로젝트에 하네스를 적용하기 위해 프로세스를 이해하고 분석하고 절차를 내 프로젝트에 맞게 수정하는 시간이 발생합니다.

**사람이 읽고 관리할 수 있어야 한다.** 

**이 문장이 에이전트의 행동을 바꾸는가?** 를 기준으로 작은 크기의 AGENTS.md, 6개의 스킬로 이루어진 작은 구조로 설계해 프로젝트에 맞게 커스텀 가능하고 사람이 관리할 수 있는 하네스를 구축했습니다.

### 2. 상태 관리

AI 에이전트를 사용하면 개발은 대화 세션을 중심으로 진행되기 때문에 무엇을 왜 바꿨는지, 현재 진행 상황과 맥락을 잃어버리기 쉽고 다시 이어받아 작업하기 어려운 문제가 있습니다.

**상태는 계획 파일에만 있다.**

계획과 승인, 검증 결과와 의사결정이 모두 한 파일에 쌓이므로 세션이 끊기든 모델이 바뀌든 그 파일의 내용만으로 맥락을 파악하고 이어받아 작업할 수 있습니다. 또한 진행 단계와 수행한 작업이 사람이 확인할 수 있는 형태로 남아 완료 후에도 재현 가능하고 추적 가능한 문서로 남기는 것을 목표로 합니다.

## 하네스 구조와 파이프라인

### 주요 파일

```
root
├── AGENTS.md                    - 핵심 규칙
├── .agents/skills/              - 스킬
│   └── ...                     
└── docs/plans/
    ├── ep-0000-template.md      - 계획 템플릿
    └── active/  done/
```

### 실행 흐름

```
     plan                         tdd          debug
      │                            │             │
      ▼                            ▼             ▼
  [계획] ────▶ [승인] ────▶ [구현] ────▶ [검증] ────▶ [완료]
                  └─────────── execute ──────────────┘
                  ▲                                  ▲
               승인 필요                           승인 필요
```

## AGENTS.md 설계 원칙

AGENTS.md 공식 가이드: [https://agents.md](https://agents.md/)

AI 에이전트가 컨텍스트에 항상 삽입되는 파일이므로 AGENTS.md에 적히는 문장이 에이전트의 행동을 바꾸는지 그렇지 않다면 불필요한 문장으로 생각하고 삭제했습니다.

> CLAUDE.md, .agents/rules/global.md는 도구별 전용 설정 파일로, 핵심 규칙이 담긴 AGENTS.md를 진입점으로 가리키도록 설정했습니다.    

### 중첩 AGENTS.md

프로젝트의 아키텍처마다 다른 기술 스택과 환경을 위한 하위 AGENTS.md를 생성합니다.

`폴더 구조`

```
root
├── AGENTS.md
├── .agents/skills/
├── docs/plans/
├── frontend         
│   └── AGENTS.md 
└── backend
    └── AGENTS.md 
```

`예시`

```
## 명령어
- 빌드: `./gradlew build -x test`
- 테스트: `./gradlew test`
- 단일 테스트: `./gradlew test --tests "*TodoServiceTest*"`
- 로컬 실행: `./gradlew bootRun`

## 구조
- 할 일 도메인과 REST API를 담당한다
- 도메인 로직: `src/main/java/**/domain/`
- 컨트롤러: `**/api/`

## 금지
- 컨트롤러에 비즈니스 로직을 두지 않는다
- 엔티티에 Lombok `@Setter`를 쓰지 않는다
```

## 스킬

실행 흐름에 필수적인 주요 스킬만 정의했습니다. 프로젝트 진행 과정 중 필요에 따라 스킬을 추가할 수 있습니다.

AI 에이전트가 임의로 스킬을 실행해 절차를 무시할 수 있기 때문에 사람이 부르는 스킬은 `disable-model-invocation: true`로 설정해 모델이 호출하지 못하게 막았습니다.

> `disable-model-invocation`은 Claude Code 이외의 다른 도구에서는 무시될 수 있습니다.      
> `execute`는 계획이 없으면 멈추고, `tdd`는 승인 전이면 절차로 되돌립니다.

`사용자 호출`

- setup — 최초 실행 시 전체 프로젝트 도메인 계획 수립. 폴더별 AGENTS.md 생성
- plan — 인터뷰로 계획 파일(ep) 작성
- execute — 계획 파일을 실제 구현
- review — `execute` 구현 이후 승인된 계획과 실제 구현을 비교 검증(새 세션에서 실행 권장)

`자동 호출`

- tdd — `execute`의 구현 단계에서 승인된 계획 안에서만 실행, 승인 전이면
  절차로 되돌린다.
- debug — 테스트 실패 후 원인이 모호할 때 실행, 계획이 완료된 이후에 발견한 버그는 새 계획 파일 작성 후 수정

## 계획 파일(ep)

하나의 목표를 위한 계획 파일을 작성하고 실행의 모든 과정을 그 파일에 기록해 세션이 끊기든 모델이 바뀌든 파일과 코드만으로 작업을 이어받을 수 있습니다. 완료된 뒤에는 이 코드가 어떤 과정에서 나왔고 무엇을 왜 그렇게 만들었는지 따라 판단할 수 있는 문서로 남습니다.

`템플릿`

```
# ep-{YYYYMMDD}-{slug}: {작업 이름}

**Status:** [Draft]
<!-- Draft → Approved → In Progress → Done.
     상태는 항상 그 상태를 만든 변경보다 먼저 기록한다. -->

> 당신은 상태가 없는 에이전트일 수 있다 — 지난 대화에 기대지 말고, 이 파일과
> 코드베이스만으로 작업을 끝낼 수 있게 쓴다.

## Purpose

<!-- 왜 이 변경이 필요한가. 끝난 뒤 무엇을 보면 동작을 확인할 수 있는가. 1-3줄. -->

## Decisions

<!-- 갈림길에서 무엇을 택했고 무엇을 버렸는가. 버린 이유가 핵심이다.
     없으면 비운다. plan이 인터뷰 중에, debug가 근본 원인을 확인한 뒤 채운다.

     - 세션 저장을 Redis로 — JWT 무상태는 즉시 무효화가 안 됨
-->

## Affected Files

<!-- 바꿀 소스 파일의 레포 루트 기준 경로. 한 줄에 하나. 폴더 전체는 끝에 / .
     여기 없는 소스 파일은 고칠 수 없다 — 먼저 추가하고 재승인받는다.
     승인된 소스의 테스트 파일은 함께 승인된 것으로 본다 — 따로 적지 않는다. -->
- src/example.ts

## Acceptance Criteria

<!-- 사람이 확인할 수 있는 구체적 조건. 확인이 끝나면 근거를 같은 줄에 남긴다.

     - [x] 잘못된 비밀번호로 로그인하면 401 — `src/auth/login.test.ts:42`
     - [x] README에 설치 안내 추가 — 수동 확인: 렌더링 확인
     - [ ] 소셜 로그인 연동
-->
- [ ] {판정 가능한 기준 1}

## Risk

<!-- execute 1단계가 위험 신호를 만났을 때만 채운다.
     전역 상태 · 공개 API 변경 · 마이그레이션 · 인증/보안 -->

(해당 없음)

## Implementation Order

<!-- execute 2단계가 채운다.
     {n}. {경로} {CREATE|MODIFY|DELETE} — {왜, 한 줄} -->

## Approval

<!-- 재승인은 새 항목이며, 마지막 항목이 유효한 승인이다.

     ### {ISO8601}
     - Approver input: "{사람의 입력 그대로}"
     - Approved files:
       - src/example.ts
     - Acceptance: {유효한 기준 사본}
-->

(아직 없음)

## Change Log

<!-- {ISO8601} {CREATE|MODIFY|DELETE} {경로} — {왜, 한 줄} -->

## Review

<!-- review 스킬이 채운다. 선택.
     verdict: APPROVED | NEEDS_REVISION
     - BLOCKER|WARNING|NOTE {file:line} — {내용} -->

(실행 안 함)

## Run History

<!-- {ISO8601} {절차} {DONE|ABORTED|FAILED} — {한 줄}
     2026-07-28T14:22:00+09:00 execute DONE — 42 tests green, build ok -->
```

<!-- 
TODO:
깃 워크트리, 랄플랜을 에드온으로 제공 
왜 승인이 필요한지는 문서 어디에도 없음
-->